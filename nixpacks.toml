[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server", "npm ci --omit=dev"]

[phases.build]
cmds = ["cd server", "npm run build"]

[start]
cmd = "cd server && node dist/server.js"
